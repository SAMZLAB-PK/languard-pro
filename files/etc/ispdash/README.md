Runtime database files are created automatically by the installer.

Do not commit real /etc/ispdash/*.db files to GitHub because they may contain private LAN information such as device names, MAC addresses, IP addresses, usage history, blocked devices, deleted devices, and limits.
